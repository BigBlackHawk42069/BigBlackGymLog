# Big Black Gym Log - Testing Phase Changelog

Version 0.9.93 - Pending

### New Features:
- **Library Page**: Added a Library view, opened from a new stacked-books icon in the SVG toolbar between Achievements and Stickerbook. Opening it grows the top panel down over the calendar, which stops painting and skips its re-renders while covered and is rebuilt once on the way out; page mode grows by the calendar's measured height and gives it back as a negative margin, so the page never changes height. At full height the glass overlay is sliced rather than stretched — only a band near the bottom with side glare stretches, so the top and bottom glare keep their normal proportions. Two pages with toolbar-docked pagination dots and arrows: the first lists the 16 training books grouped into Stat Books, Gym Gains, Energy, and Happy, each with a short description and a data column; the second is a two-column checklist of every other book. Every row takes an equal share of the height with no scrolling, and compact trims descriptions and abbreviates numbers. Unread books are greyed, books being read show a Reading marker, and finished books get a check.
- **Book Tracking & Training Book Gains**: Book use (2050) and book finish (2051) logs are now synced, backfilled, and kept through export/import, stored as item entries with the book's ID. From those, each training book's data is calculated from the log: stat books show the jump in their stat at the finish, measured between the gym trains either side of it (or today's live stat) less any stat enhancers used in between, flagged when it doesn't match the expected +5%; gym-gain books show only the extra gains their bonus produced over the reading window (`gain × bonus / (1 + bonus)` per train); energy and happy books show every gain made while they were active. Multi-stat books only show the stats actually trained plus a total. Memories And Mammaries takes the effect of the book read before it over its own 31 days (or pays a stat book out 31 days after use); when that's a training book it gets its own row at the bottom of the first page, tinted gold along with the book it repeated. Values from a book started before the log began are marked as possibly incomplete.

### Bug Fixes:
- **Faction History Never Loading**: The Create API Key link never requested the Faction log category, so the faction application log Big Black Backfill reads to line up past factions' ranked wars was always refused and skipped silently. The key now requests it. Existing keys need the Faction log category added in Torn's API settings to pick this up.

### Improvements:
- **Weekly Bar Graphics Overhaul**: Redrew the weekly capsule bar to look like a physical, higher-fidelity piece of hardware. Each of the five bays is now a rounded glass tube with layered highlight and shadow bands, held in place by shaped metal brackets top and bottom instead of flat hatched rails, and the bays are joined by recessed metal couplings rather than sitting in one flat housing. The rails and end terminals were slimmed down so the tubes take up more of the bar, the bar's end caps pick up the same metal finish as the level bar, and empty bays show a dark tube recess. Completed weeks' glow was tightened to hug each tube instead of spilling into its neighbors, and the animated shine is clipped to the glass so it no longer runs over the brackets. The handle tab at the end of each bar now holds the week's summary chart in its own recessed inset; at rest the chart is scaled up to fill most of that inset, returning to its normal size when the bar is hovered or being viewed, and compact mode's raised tab was shortened to remove the empty space above the chart.
- **Stickerbook Layout**: Gave the sticker grid more room. The page title strip under the grid was shortened by 3-4px in every panel mode, with the title moved down to match, so the grid band is taller (page mode's stickers grow into it when height is what's limiting them). In the expanded panel, the grid now spreads out into the arrow gutters as the panel narrows (none at full width, up to 75% of each gutter at the narrowest), so stickers shrink later and more slowly; only the outer columns reach the gutters, and their stickers sit in the top and bottom rows above and below the arrows. The page arrows are now centred on the gap between the two sticker rows instead of partway down the panel: they were positioned against a leftover 12px bottom offset rather than the space actually reserved for the title, which left them a few pixels below the grid's centre in compact and expanded modes.
- **API Key Scope**: The key's log categories are now defined once in the constants and shared by both Create API Key buttons. Dropped the Points category, since the points energy refill log it was requested for is already covered by Refills, and added Books for the book finish log. Book use and finish logs ride along on the existing sync and backfill calls that had room for them, so tracking books adds no extra requests. Existing custom keys need the Books log category added in Torn's API settings, or syncing will fail until they do.
- **Copy/Paste Output Refinement**: Reworked the clipboard text from the ledger and achievements pages for consistency. Every copy now leaves a blank line after its header, and the ledger copy's header reads Big Black Gym Log. Stat labels come before their numbers everywhere (Greatest Gains, streaks, Happy Jumps, enhancers). Lists that ran onto one line and wrapped awkwardly when pasted (streak gains, Best Happy Jump) now put each stat on its own line. Endocrine Enhancers and Happy Helpers space out their entries and leave out items that were never used, and overdose lines show both lost happy and energy. Empty values read `+0` for gains and `0` elsewhere instead of `—`. Layout copies were brought in line with their section versions: streak rows indent their gains, the Greatest Gains column copy labels each period, Best Happy Jump includes its date and time, and streak stat cells include the day count.
- **Toolbar Spacing for a Fifth Icon**: Tightened toolbar spacing so the new Library icon fits without resizing anything. Compact's icon gap dropped from 11px to 9px, the same floor the other modes use; page mode's smallest size (385px and below) uses an 8px inset and gap; and the expanded panel on screens under 380px scales its icons and gaps down together. All of these apply in every view, so the icons sit in the same place whichever page is open, and the extra clearance taken before the graph's stat pills is limited to the graph view.
- **Instant Panel Expand/Collapse**: The panel now snaps straight to its expanded or compact size instead of growing and shrinking into it. Nearly every size inside the panel is derived from the panel's own width, so the old width/height animation recalculated and re-laid-out the entire panel contents on every frame of the transition, which was especially heavy with the ranks page open. The per-element size transitions inside the panel (header height and padding, item viewer offset, ledger and header font sizes, flex/max-height changes, month header spacing) were removed along with it, so the contents switch sizes in the same instant as the panel.
- **Titles Page Layout Pass**: Reworked the measuring pass that sizes the titles page after it's built (star sizes, title card trim, rank bar placement, and the level-bar tooltip's title size). Every write now only lands when its value actually changed — a same-value write still invalidated the page's styles, forced the next measurement to recalculate the whole page, and could retrigger the pass's own resize watcher, including a variable written on the document root that invalidated all of Torn's page. Column measurements are now all read before any are written instead of alternating, which had forced a full recalculation per column; the rank bar's pixel-snapping now predicts its landing position from the current layout and writes once instead of writing, re-measuring, and writing again; and the resize watcher coalesces multiple notifications into a single pass per frame. Also removed the retired per-block SVG neon frame (long since replaced by the static plate outline and hidden) along with all the measuring and path-building the pass was still doing for it every time.
- **In-Place Title Picks**: Picking a title (clicking stars, the reset arrow, or the dev title override) now updates the star highlights and the identity card's title text in place instead of tearing down and rebuilding the entire titles page and re-running its full layout pass on every click. It falls back to the full rebuild whenever anything besides the pick itself has changed.
- **Rank Readout Animation Sync Scope**: When the live rank readout updates in place, the shared animation-timeline variable is now stamped only on the two parts it replaces (the rank labels and the card's rank badge) instead of on the whole achievements container, which restyled every element on the page and re-timed every running animation. A bricked-state or rank finish/material change, which switches animations on for elements outside those parts, still takes the container-wide stamp.
- **Calendar Shine Animations on the GPU**: Reworked the hover shine on gold, green, and diamond jewels and on stickers. Each shine animated its gradient's background position underneath an image mask and a blend mode, which forced the gradient to be redrawn, masked, and re-blended every frame for the full length of the animation. The gradient now sits on an inner band sized to cover the same travel and slides with a transform, so it's drawn once and only moved; the fades keep their original keyframes and easing on the outer element. The bands are kept to the minimum size their travel needs, since the whole band is rasterized when the shine starts.
- **Calendar Repaint Isolation**: Stopped hover effects on one part of the calendar from repainting the rest of it. The grid was one shared stacking context, so a layer appearing anywhere (a shine starting, a post-it peeling, a weekly bar sweeping) made the browser regroup and repaint everything drawn after it — later cells in the week, every week below, and the calendar content scrolled under the header. Each day cell and weekly bar track is now its own stacking context, each week (its row of cells plus its bar) is grouped into its own isolated container, and cells and weeks each sit on their own layer, so hovering only repaints what's actually under the pointer. The tooltip, header summary buttons, header month/year labels, month arrows, and header level bar received the same treatment against the header's own hover effects. Weekly bar sweeps now only hold a layer while their row is active rather than permanently.
- **Hover Intent**: Hover effects now wait for the pointer to rest on something for 60ms before starting — tooltips everywhere, the calendar cells' shine, post-it peel, and day-number highlight, and the weekly bar's handle, glow, and sweep. Sweeping the mouse quickly across the calendar crossed each cell in a few milliseconds and started all of that work for every cell it passed; it now starts none of it, while ordinary hovering behaves the same after a barely noticeable pause. A tooltip already on screen stays up while passing over other targets instead of flickering between neighbors.
- **Tooltip Rebuild & Measurement**: The tooltip no longer rebuilds its contents when asked to show the same content it's already holding, and remembers its measured size per tooltip so revisiting the same cell, bar, or graph point skips the two forced layout measurements it took to size and position itself. Cached sizes reset on window resize, web font loading, and changes to the level-bar tooltip's title size, and tooltips with images still loading are never cached.
- **Page Watchers Ignoring BBGL's Own Changes**: The two page-wide DOM watchers that detect Torn rebuilding its page (to keep BBGL's injected buttons and the chat-aware panel positioning intact) now skip batches of changes that happened entirely inside BBGL's own panel or tooltip. They were re-running their checks on every tooltip content swap and calendar/graph re-render. Anything Torn does, including removing or moving BBGL's injected elements, is still handled exactly as before.
- **Graph Hover Dot**: The graph's highlighted point dot now appears instantly instead of fading in over 0.1s. Each fade started a new animation on an SVG shape, which regrouped and repainted every line and point drawn after it, so dragging along a line repainted most of the graph on nearly every step.
- **Rank Name Shine Layers**: Animated rank names (emerald, gold, diamond) on the rank scale and identity card now sit on their own layer. Their shine is clipped to the letters and can't be moved to a transform, so it still redraws each frame, but now only the name itself redraws instead of the plaque grain, rivets, frame bands, and rank track under its glow.
- **Header Summary Button Alignment**: The All-Time, year, and month summary buttons beside the calendar header labels no longer shift away from their labels when hovered or active. The hover/active state used its own vertical offset for each panel mode (and a separate correction for the year row) instead of the resting one, so on hover the buttons dropped anywhere from half a pixel to five pixels depending on the row and mode — month happened to come closest, while year and All-Time visibly sank below their labels. Hover and active now keep the resting offset and only scale up from the button's bottom edge, so a button stays level with its label in every row and mode. The whole header label cluster was also nudged up 2px. In page mode, the year and month labels were lowered to sit on the bottom of their summary buttons, the gap between rows was tightened by 4px at every panel width, and the All-Time row was pulled a further 2px closer to the year row, with the month row staying anchored in place.- **Cheaper Hover Selectors**: Replaced the calendar post-it peel's and level-bar tooltip's `:has()` selectors with plain classes set when the cell or tooltip is built, and narrowed the day-number circle's `transition: all` to the properties it actually animates, which stops every cell hover from starting an invisible transform transition.
- **Asset Format Conversion**: Converted the calendar reward/event icons, the sticker-frame and sticker-background art, and all 20 Gym/Casino stickerbook collectibles from PNG/JPEG to WebP, cutting their combined size by roughly 45% with no visible quality change — lossless WebP for anything with hard alpha edges (icons, stickers), lossy for the photographic backgrounds where it measurably shrank the file. Three calendar grid-tint JPEGs were left as-is after WebP re-encoding came out larger than their already-optimized JPEG source.
--------------------------------------------------

Version 0.9.92 - 2026-09-11

### New Features:
- **Level-Band Title System**: Replaced the single flat flavor title per Atrophy tier with a six-band system that changes the title as you climb through each tier instead of staying static for the whole run. Titles follow a raw-clay-to-fired-brick metaphor (Dry Clay → Moistened Clay → Hand-Jerked Clay → Block-Molded Clay → Pit-Fired Clay → the tier's capstone title), escalating in wording intensity per Atrophy tier, with a fixed easter-egg title at Level 69 on every tier. Only reaching Level 100 on the final Atrophy tier (A2) shows "Fully Bricked" — A0/A1 auto-roll into the next tier and keep their own capstone title instead.
- **Stat-Based Flavor Title**: Added a second title system with 10 independently unlocked word tiers for each battle stat, driven by cumulative energy spent training that stat after the reward start date. Every tier supplies an adjective and noun; the player can combine any two unlocked words, including words from different stats, while earned mode follows the two stats that most recently advanced and keeps the higher battle stat in the noun slot. Each word carries its own tier finish from dull silver through green, gold, and an iridescent capstone.

### Bug Fixes:
- **Sidebar Button Inheriting Torn's Native Alert Flag**: Fixed the Gym Log sidebar/flyout/mobile-bar button visually lighting up with Torn's native blue "attention" indicator whenever the real Gym nav icon had one. The button's live style-copy logic mirrors whatever classes sit on Torn's native container so it survives hash rotations, but it wasn't excluding Torn's attention-state class the way it already excluded the active-state one, so the flag rode along unfiltered onto our own button on desktop, mobile, and flyout alike.
- **Sidebar/Footer Buttons Disappearing Until Reload**: Fixed a reliability bug where the Gym Log sidebar and footer buttons could vanish entirely and never come back without a full page reload. The DOM-mutation observer downgraded itself from watching the whole page to a handful of narrow per-parent observers once the buttons were confirmed present, as a performance optimization; if Torn's SPA replaced one of those watched parent elements wholesale instead of just mutating its children — which the newer Fly-Out Sidebar does on every open/close — the narrowed observer was left watching a dead, detached node and never fired again, permanently disabling the button's own recovery path. Removed the downgrade entirely; the observer now stays permanently active, which an existing per-frame debounce already keeps cheap.
- **Cross-Device Training Data Loss**: Fixed a bug where a train click could permanently drop unsynced training data from other stats trained on another device. The sync window's "how far back to check" marker was shared across an entire log group rather than tracked per stat/item, so clicking Train — which only ever checks the one stat you clicked — advanced that shared marker as if all four stats had just been checked, silently pushing the window past anything still unsynced on the other three. It's now tracked per individual stat/item instead, so a narrower call can never advance the window further than what it actually looked at.
- **Imported Training History Scoring Zero Career EXP**: Fixed a bug where any log entry brought in through Import lost its training-log identity, causing every imported day to contribute 0 career EXP toward your level — even though the same import's stats, calendar, and stickers all displayed correctly. The export format never wrote an explicit "this is a training entry" tag on gym rows (it's ordinarily inferred from context), and reversing that process on Import never reconstructed it, so the reward system's gym-entry filter matched nothing across the entire imported history. Both the export/import round trip and every normal page load now stamp this tag consistently, so entries already sitting in an affected log self-heal on the next reload — no re-import required.
- **Duplicate Item Log Entries After Resyncing an Imported Log**: Fixed a bug where resyncing shortly after an Import could duplicate item-log entries (Xanax, energy items, overdoses, etc.) for anything used within the resynced window. The duplicate-detection check identified a synced item entry by Torn's own internal log ID, but that ID is dropped during Export — so a re-fetched entry could never be recognized as "the same one" already imported, and both copies were kept. Item matching now uses the same (timestamp + item) key already used elsewhere in the log for this exact purpose, which survives an export/import round trip intact.
- **Data Gap Between an Import and the Next Sync**: Fixed a bug where a stretch of real training history could go permanently unrecorded following an Import, with no indication anything was missing. The live sync progress marker is intentionally left out of an Export (it would be stale or wrong on another device), but leaving it unset meant the very next sync after an Import asked Torn for logs with no starting point at all — which returns only a recent slice of history, not everything, the same reason the Backfill feature exists in the first place. Anything between the import's last recorded entry and wherever that slice happened to start was neither in the import nor fetched by the sync, and nothing would ever go back for it afterward. The sync marker is now re-anchored to the import's own most recent entry, so the very next sync picks up exactly where the import leaves off instead of guessing.
- **BBGL Icon Styling Leaking Onto Torn's Nav Items**: Fixed the white fill and glow BBGL applies to its own Gym Log sidebar icon also being applied to Torn's native sidebar entries. The rule was written against the generic container and active-state class shapes without ever anchoring to BBGL's own elements, so it matched whichever nav item Torn had marked active on any page — the same class of issue as the Native Torn Button Conflict fixed in 0.9.90.
- **Sidebar Active-State Glow Removed**: Removed the white fill/glow the Gym Log sidebar icon showed while its page was active. Torn has since removed the equivalent white highlight from its own native sidebar icons, so BBGL's icon was left as the only one on the bar still doing it. The purple "something's new" update indicator is unaffected and still lights the icon and its dot badge exactly as before — this only removes the separate white active-page glow. The icon's selected state is now conveyed purely by Torn's own native bar/background highlight (already reapplied to our entry), with no BBGL-specific styling layered on top.
- **Graph Hover Handler Accumulation**: Fixed the graph leaving a dead mouse-leave handler attached to its container on every redraw — toggling a stat, switching modes, resizing, or changing the selected period. The cleanup that runs ahead of each redraw was detaching the wrong function for that one event, so nothing was ever actually removed and the handlers stacked for as long as the graph stayed open, each one sweeping the chart again whenever the pointer left it.

### Improvements:
- **0.9.92 Reward Gate**: Added a versioned reward-only reset that advances `rewardStartDate` for users updating from before 0.9.92 without deleting their history, settings, or API key. The cutoff records its own applied-version marker so it runs once even if the changelog version marker remains old, and imports from before the gate receive the same new reward start rather than restoring pre-gate reward progress.
- **Leveling Curve Retune**: Retuned the per-level EXP cost curve (floor, peak, breakpoint percentages/values, and tail-curve steepness) and the daily EXP earn-rate tiers based on live feedback, replacing the placeholder values shipped in 0.9.90. The Gold+ earn rate (1,500E+) now diminishes sharply, offset by a flat +100 EXP bonus for actually reaching Diamond (2,000E+) — a risk/reward tradeoff where pushing past Gold only pays off if you go all the way. Also adjusted the Happy Jump burst rate.
- **Per-Atrophy Level Range**: Reworked the leveling engine so every Atrophy tier now caps at the same Level 100, but starts at a different point (A0 at Level 0, A1 at Level -1, A2 at Level -10) — later Atrophy tiers are genuinely longer climbs (more paid level-ups) on top of their existing per-level cost multiplier, not just costlier per level.
- **Graph Y-Axis Precision Refinements**: Reworked y-axis gridline label formatting across all graph modes (Values, Gains, Rates). Abbreviated labels (k/m/b/t/q) now drop their decimal once a value reaches 100+ units of its tier (e.g. `102m` instead of `102.3m`), falling back to an exact half-unit (`102.5m`) only when whole-unit gridline spacing would otherwise leave just 2 labels on the axis. A step-aware pass also strips the decimal from an axis's entire label set whenever none of them actually land on a non-whole value (e.g. `82m, 84m, 86m` instead of `82.0m, 84.0m, 86.0m`), rather than relying on the magnitude rule alone. Also tightened the "extra headroom" gridline threshold from 5% to 1% of the axis range, so a padding gridline above the plotted line's peak only appears when the peak is genuinely close to the top of the chart instead of triggering on ordinary near-round numbers. Additionally, the Rates-mode tooltip now drops decimal places on its Rate and Growth values once they exceed 99.
- **Sidebar Notification Flag Overhaul**: Reworked the purple "something's new" indicator shown on the Gym Log sidebar button. The icon now fills with a two-stop purple gradient (mirroring the same technique Torn uses for its own native blue attention icons) instead of a flat color, and gained a small dot badge matching Torn's own attention-dot styling and positioning — right-aligned and vertically centered on the desktop sidebar and Fly-Out Sidebar rows, corner-inset on the mobile top bar. Also removed a glow/drop-shadow that was applying to the flagged icon regardless of whether the button was actually the active page, and stopped the existing purple background tint from applying to the mobile top bar button, which was never meant to have any background chrome — icon and dot only, matching its icon-only presentation everywhere else.
- **Conditional Background Sync**: Reworked automatic background log syncing to only run while there's actually a reason to — the panel is open in any view (including the gym log page), or you're on the gym page — instead of firing on a fixed timer regardless of what you're doing. A lightweight check runs every few seconds and only ever contacts the server once the last full sync is genuinely 30+ minutes old, so training done elsewhere (mobile, another device) shows up automatically the next time you're looking at the log, without needing to click anything. Training itself — clicking Train, or the passive check while just sitting on the gym page — now only fetches what the exp bar needs (all four stat logs plus Ecstasy, so a Happy Jump's bonus exp is caught immediately whether it happened here or is being picked up from elsewhere), instead of the heavier reconcile call used previously for both. That heavier reconcile (items, current stats, overdoses, and a conditional stat-enhancer check) now only runs when it's actually needed: on the routine 30-minute background check while the panel's open, or immediately — skipping that 30-minute wait — the next time the panel or gym log page is opened after training, even across a device switch or closing the browser entirely in between. The automatic full sync that used to fire the moment you left the gym page is gone; it's replaced entirely by this on-demand catch-up, so nothing gets fetched until something is actually going to be looked at. When that catch-up happens silently in the background, the exp bar snaps straight to the correct value instead of playing a level-up animation you never triggered, which stays reserved for exp you just earned by clicking Train.
- **Per-Account Sticker Placement**: Reworked how the sticker roulette assigns which sticker appears on a given day. It previously advanced through a shared coprime-stride rotation, so every account landed on the same relative order of stickers, just offset by how quickly stickers unlocked — different logs could still end up looking identically patterned. Placement is now derived from the account's own player ID, so it's unique per account while staying perfectly consistent across that account's own devices with no syncing required. Also added a cap preventing the same sticker from landing more than twice in a row wherever the unlocked pool is large enough to avoid it.
- **Gym Page EXP Bar Repositioning**: Moved the EXP/level bar on the gym page from the very top of the page down to sit directly above the training stat cards, just above the gray content panel rather than floating above everything. Also slightly increased the bar's height for a bit more visual presence, without affecting the crown badge's size.
- **Gym Page EXP Bar Crown Clearance**: Slightly increased the top margin above the gym page's EXP/level bar container so the crown badge, which grows upward from the track and clips against the container's own top edge, has a bit more breathing room before it touches the ceiling above it.
- **Tall Mode Header Decoupling**: Fixed the calendar/bottom panel visibly sliding a couple pixels whenever Tall Mode was toggled in the expanded panel. The header area's growth was previously offset with a hand-tuned negative margin sized to approximately cancel its flex-layout growth — in compact mode it happened to cancel almost exactly, but only because a width-relative percentage coincidentally landed close to the actual height-based growth amount; in expanded mode the flat pixel value it used was never quite right, which is what produced the visible slide. The header is now positioned independently of the panels beneath it, so its height can change in Tall Mode without ever displacing them, in every panel size and dock state.
- **Ledger Vertical Centering**: The ledger's stat columns (STR/DEF/SPD/DEX) now center their content automatically regardless of whether the optional Rates row is showing, instead of relying on hand-tuned padding that only looked centered for one specific row count and drifted whenever Rates was toggled.
- **Network Layer Consolidation**: Consolidated every Torn API request in the script — live sync, Backfill's page-by-page scan, faction war history, and the account-discard baseline check — through a single shared request function, removing several duplicated fetch/error-handling blocks that had drifted independently across the sync and Backfill engines. No behavior change; a maintainability pass only.
- **Graph Hover Responsiveness**: Reworked how the graph tracks the pointer while hovering or scrubbing. It previously ran a full nearest-point search on every raw pointer event rather than once per drawn frame — on a high-refresh mouse that's several complete passes per frame, all but the last of them discarded. Each pass also re-collected and re-parsed the position of every plotted point (one per data point per active stat, which on a full-history view runs to thousands), and rebuilt the tooltip from scratch even when the pointer hadn't left the point it was already sitting on. Point positions are now captured once when the graph draws, the search is coalesced to one per frame, and the tooltip only rebuilds when the highlighted point genuinely changes. Touch scrubbing behaves exactly as before — the part that suppresses page scrolling still runs immediately on the real touch, ahead of the coalescing.
- **Panel Positioning Recalculation**: Reworked when and how often the panel recalculates its position against Torn's chat and native Notes/People/Settings panels. Each trigger was running two to three redundant sweeps of the page's open windows, every one of them forcing the browser to stop and recompute layout mid-pass; the whole routine ran in full even while the panel was closed, measuring the page in order to position something that wasn't on screen; and the page-wide watcher that triggers it re-scanned its own results for every change in a burst even when a recalculation was already queued for the next frame. Because Torn's chat drives that watcher continuously, all of this was running in the background whether or not the log was ever opened. The redundant sweeps are gone, a closed panel now does only the part that's actually observable (releasing the chat shove and clearing the footer tab's active state), and the watcher stands down once a recalculation is already pending. Nothing about where the panel or chat windows end up has changed.
- **Calendar Rendering**: Reworked two sources of repeated work in the calendar grid. Every render rebuilt the hover tooltip markup for all 42 day cells up front — a detailed stat breakdown per cell, generated in full to show one at a time — and separately recomputed the same handful of shared values (today's date, war markers, the log's start date) inside each of those 42 cells. Tooltips are now built the first time a cell is actually hovered and reused from then on, and the shared values are computed once per render. Since the grid re-renders on every data update, month change, and panel open, this removes the largest single burst of work in the panel; it also spreads the tooltip cost across ordinary hovering rather than concentrating it into the render itself.
- **Sidebar Active-State Checks**: Reworked the routine that keeps the Gym Log button's highlight in sync with the current page, which runs on every DOM change Torn makes anywhere on the site. It was performing two page-wide attribute scans each time: one to discover the build-specific class Torn uses to mark the active nav item, and another to clear that state off the other entries while viewing the gym log page. The first now stops once it has found what it's looking for, since that class is fixed for as long as the page is loaded, and the second looks elements up directly by the now-known class instead of walking every nav element and its contents.
- **Achievements & Stickerbook Pagination Redesign**: Moved the achievements page's pagination dots and prev/next arrows out of their own bar at the bottom of the panel and into the SVG toolbar row at the top, dynamically centered against whichever of the ledger/graph/achievements/stickerbook icons are actually on screen rather than a fixed offset — keeps it correctly aligned across every panel size and mode instead of needing separate hand-tuned positioning per mode. Dots and arrows both gained larger tap targets and a hover treatment (a brightness step on the dots, a glow on the arrows) without changing their resting size. The prev/next arrows are now a small inline SVG chevron rather than a Unicode "❮"/"❯" glyph, since that character's own ink doesn't sit centered in its line-box the way ordinary text does — invisible until these needed pixel-precise alignment against the dots rather than just sitting on their own. The stickerbook's own pagination (previously a plain row of dots with no arrows) now carries the identical treatment, added alongside its existing big edge arrows rather than replacing them. This also folded the stickerbook's sponsorship/gold page into the same navigable sequence as every other page — it was previously a special-cased detour reachable only via swipe or its own separate dedicated button, rendered through its own parallel (and, in places, inconsistent) code path; it's now an ordinary page at the front of the range, reachable from any prev arrow or dot like any other page, with its gold treatment applied as a plain CSS variant instead of separate markup.
- **Always-On Tall Header**: Removed the +/- toggle that switched the panel header between its short and tall states; the header is now always tall, matching how the gym log page already presented it. Discoverability was the reason — the view-switcher icons, item counters, and copy button only ever appeared once the header was expanded, so anyone who never found the toggle never found them either. Removing the mode took the toggle button with it, along with its click handler, the tall/short state that was saved and synced between open tabs, and the separate logic that had to silently force the header open whenever the panel was restored straight into the graph, stickerbook, or achievements view. On the styling side it collapsed a duplicate set of header height variables and eighteen tall-specific rules back into the base ones they were shadowing.
- **Dedicated SVG Toolbar Container**: The toolbar row at the top of the panel (view-switcher icons, item counters, copy button) now lives in a container of its own instead of every control being positioned individually against the header. Its height is one value per panel size, derived from the tallest icon it has to hold plus matching clearance above and below, and everything inside centers against that single line — previously the icons shared a flat top offset, which left the smaller ones sitting fractionally high. The icons themselves are now an evenly spaced row driven by one gap value, replacing twenty hand-tuned horizontal positions spread across five rule sets that each had to be re-tuned per panel size and mode. Added a soft dark gradient behind the row that fades out completely by the container's bottom edge, so the toolbar reads as its own band without a hard border under it.
- **Graph Controls Moved Into the Toolbar**: Moved the graph's Gains/Rates and STR/DEF/SPD/DEX/TOT pills out of the top of the graph body and into the toolbar row, where they appear only while the graph is open. The mode pills sit with the view-switcher icons behind a hairline divider marking where the view switcher ends and the graph's own controls begin; the stat pills are justified to the opposite end of the row. Because the two groups sit at opposite ends, the space between them absorbs whatever width difference each panel size produces, so nothing has to be measured or positioned per mode. With the pills out of the graph body, the chart itself now uses the full height of its container rather than leaving a strip reserved above it.
- **Graph Plot Boundaries**: Reworked the box the graph draws into so it is defined against the toolbar above it rather than by numbers of its own. Its left and right insets are now the same inset the toolbar's contents use, so graph content starts on the left edge of the leftmost toolbar icon and the plot reaches all the way to where the last stat pill ends; its ceiling sits on the bottom edge of the toolbar band; and its floor is a small flat padding in every mode. This replaced three separate padding rules for compact, expanded, and page mode that had drifted out of agreement with each other, and along the way caught page mode subtracting the wrong header padding — it grows its own with panel width instead of holding the shared base value, which would have left the graph's ceiling misaligned by up to 18px at wide page sizes.
- **Graph Y-Axis Label Alignment**: Halved the gap between the y-axis labels and their gridlines, and set the labels to sit flush on the lines rather than being straddled by them — previously only the expanded panel was flush, while compact and page mode had the line running through the text. The labels are also left-aligned as a column now, so when one long label forces the column wider, the extra space opens between the shorter labels and their lines instead of between those labels and the panel wall. A label reading exactly zero is the single exception and stays tucked against its own gridline. Supporting this, the column's width is now measured from the labels' real rendered width rather than estimated from character count; the estimate was fine while the labels were right-aligned, where reading it short only cost slack against the wall, but left-aligned that same error would push the widest label into the gridlines.

--------------------------------------------------

Version 0.9.91 - Pending

### Bug Fixes:
- **Fly-Out Sidebar Button Styling**: Fixed the Gym Log sidebar button rendering unstyled (missing background pill, wrong link classes) on PC. Torn's new "Fly-Out Sidebar" account setting changes the desktop sidebar's underlying CSS class names even while actually browsing on PC — where the flyout itself never renders — and the button's live style-copy logic wasn't accounting for the new class shape, including a container class and row class it was silently dropping.
- **War Win/Loss Post-Its Not Appearing**: Fixed ranked-war win/loss markers never appearing on the calendar. The faction-ID lookup used to tag war outcomes was calling `user/?selections=faction`, which is an API v2-only selection and always errors out under v1, silently failing the lookup every time. Reverted to resolving the faction ID from the same `faction/?selections=rankedwars,basic` request already used to fetch the wars themselves. Requires generating a new API key via the updated Create API Key flow to pick up the added "basic" permission.
- **Expanded Panel Ledger Squeeze on Mobile**: Fixed the ledger area visually compressing whenever the expanded panel's height got constrained (e.g. a short mobile viewport forcing `max-height` to clamp below the panel's normal height). The ledger's height was a percentage of the panel's own height, so it shrank right along with it even though `flex-shrink` was already 0 — percentage flex-basis isn't protected by that the way a fixed value is. It's now a fixed pixel height that holds steady regardless of clamping, so the scrollable calendar/grid area beneath it absorbs the height reduction instead, since it already scrolls and the ledger doesn't.

### Improvements:
- **Fly-Out Sidebar Support**: Added the Gym Log button to Torn's new mobile Fly-Out Sidebar, alongside its existing placement in the desktop sidebar and footer tab.
- **Calendar Header CSS Cleanup**: Reworked padding and alignment handling across the calendar header (month/year/all-time summary rows, title stack, header wrapper) to remove several stacked/duplicated padding values and brute-forced positioning offsets left over from earlier layout passes, improving alignment consistency across panel widths and modes.
- **Footer Tab Tap Tooltip Suppression**: Suppressed the Big Black Gym Log tooltip from appearing when tapping (rather than hovering) the footer tab on mobile. It's now hover-only, matching desktop behavior, since tapping the footer tab already performs an immediate action (opening the panel) and didn't need the tap-to-show tooltip treatment used elsewhere.
- **Toolbar & Footer Tab Mobile Tooltip Behavior**: Reworked touch tooltip handling for the ledger/graph/achievements/stickers toolbar and the footer tab. Tap-and-hold on the toolbar now previews the tooltip like a real hover (via the existing panel-wide scrub system) and releases cleanly without also triggering the view switch; a quick tap still switches views but now also shows the tooltip briefly (500ms auto-dismiss) instead of leaving it open indefinitely. The footer tab was excluded from tap-triggered tooltips entirely, since tapping it performs an immediate action (opening the panel) and doesn't need one.

--------------------------------------------------

Version 0.9.90 - Pending

### New Features:
- **RPG Leveling System**: Introduced the career leveling system built on top of the existing weekly-points foundation. Daily training now accumulates career EXP (tiered by E spent, with a Happy Jump bonus) into a 1–99 per-level cost curve, gated across three Atrophy tiers (A0/A1/A2) with independent floor/ceiling values and per-tier multipliers. The cost curve itself (piecewise linear ramp into a power-curve tail) is fully locked in and pending live user feedback before final tuning. Added level-up animations to the level bar; Atrophy-tier transition animations are in progress.
- **Level 100 Locked Achievements Page**: Added a 6th page to the achievements panel, gated behind reaching Level 100 (the first Atrophy tier). Displays a locked placeholder for anyone below that level.
- **Dynamic Summary Bar-Graph Buttons**: Replaced the static All-Time/Yearly/Monthly summary buttons with dynamic bar-graph icon buttons.
- **Expanded Item Tracking & Calendar Events**: Expanded training item tracking to include Overdoses (ODs), and added war status/events directly to the calendar so events affecting training are visible at a glance. Requires a broader API key scope to capture the additional data — existing users can update this via the existing Create API Key flow in settings.
- **Big Black Backfill Onboarding Overhaul**: Rewrote the entire Backfill onboarding and scan-status experience end to end. The Start Tracking modal now frames the fresh-log vs. Backfill choice in plainer terms ("START EMPTY LOG" replaces "START LOG FRESH"). The scan overlay's masked states were rewritten with reason-specific copy for every stop condition, splitting the previous single "Scan Interrupted" catch-all into a distinct `interrupted` stopReason (tab/browser closed mid-scan) versus `error` (in-session network/API failure), alongside dedicated Paused, Daily Limit Reached, and Fully Backfilled states with an italicized reward-gating note. The scanning state gained an inline pause control next to the title, a pulsing "rows recovered" indicator instead of a bare ticking number, a Fjalla One title font matching the rest of the panel's branding, and a repositioned Cancel button. The Settings menu's Big Black Backfill button no longer abbreviates to "BB Backfill" in compact mode (aside from the Resume state, which still needs the shorter form), requires a tap-to-confirm gate before starting or resuming a scan, surfaces its daily-cap cooldown via a hover/tap tooltip instead of a live per-second countdown on the button face, and gains a permanent green "Fully Backfilled!" state that persists until the log is cleared.

### Bug Fixes:
- **Native Torn Button Conflict**: Fixed a site-wide button misshaping issue caused by BBGL's custom button styles being applied globally to Torn's native `.torn-btn` class. All BBGL-generated buttons have been migrated to a private `.bbgl-btn` class family (`.bbgl-btn`, `.bbgl-btn-green`, `.bbgl-btn-red`, `.bbgl-btn-purple`), keeping button appearance identical while fully isolating BBGL's styles from Torn's UI on every page.
- **Premature Sticker Unlock**: Fixed a bug where stickers were appearing in the stickerbook as soon as the weekly bar filled up mid-week. The sticker award logic now only processes fully completed past weeks, so stickers are never unlocked for the current active week regardless of bar fill state — they appear correctly once the week ends and is archived.
- **Automatic Log Sync Speed**: Improved automatic log syncing so training done on another device reflects more quickly.
- **Backfill Interruption Data Loss**: Fixed a bug where an interrupted Backfill run could silently lose log data without any visible indication to the user.
- **Sidebar Active-State on Gym Log Page**: Fixed a bug where the Gym Log sidebar button wouldn't show its active/highlighted state while already viewing the gym log page.
- **Incomplete Clear Data Reset**: Fixed a bug where clearing the log left certain pieces of data behind, which could distort future data going forward.
- **Achievements Page Settings Return**: Fixed a bug where closing Settings while viewing the Achievements page dropped you back on the ledger/calendar instead of returning to Achievements, unlike Graph and Stickerbook which already remembered their place.

### Improvements:
- **Weekly Progress Bar Redesign**: Replaced the solid weekly progress bar fill with individual daily capsules for clearer at-a-glance discoverability. Summary buttons were redesigned as visual bar-graph icons and now also appear as flags directly on the weekly progress bar, indicating when a weekly summary is available.
- **Simplified Disclosures**: Streamlined the onboarding privacy/consent disclosures for a more frictionless experience. Added a separate Technical Disclosure with the full detailed breakdown for users who want it.
- **Settings Menu Reorganization**: Reorganized and simplified the settings menu layout.
- **RESYNC Button**: Moved the log refresh action out of its previous location into a dedicated RESYNC button at the top of the settings menu for quicker access.
- **Achievements Page Tooltip Refinement**: Rewrote tooltips across the Endocrine Enhancers and Happy Hopping achievement pages — corrected item pluralization, refined the wording and formatting, and made expanded mode show additional category detail not shown in compact/page mode.
- **Endocrine Enhancers Period Toggle**: Added an All-Time / Selected switch to the Endocrine Enhancers achievements page, letting users choose between lifetime totals and the period currently selected on the calendar.
- **Performance Optimizations**: Additional performance passes to keep the script lightweight.
- **Demo Mode Bar Repositioning**: Moved the purple Demo Mode bar from the bottom of the calendar header to the top, clearing room for the new EXP/level bar now anchored to the header's bottom edge. In panel mode, the bar now stays pinned to the bottom edge of the top panel as you scroll or toggle tall mode instead of scrolling out of view.

--------------------------------------------------

Version 0.9.75 - 2026-06-05

### New Features:
- **BB Backfill**: A powerful historical reconstruction engine that allows you to walk back and synchronize your training history, reaching all the way to the creation of your account. By intelligently scanning and parsing your training logs in reverse, it bridges missing data gaps to construct a complete, seamless lifetime record of your progression at your own pace.
- **BB Best Gym**: Added an optional feature (on by default) that automatically switches to the best gym for the specific stat you're training, preventing accidental training at suboptimal gyms. Pressing a train button while a better gym is available opens Torn's gym change for that gym so you can confirm or cancel it — it never trains or switches without you. Once it has offered a swap for a stat, it stays out of the way for that stat until the page reloads, so you can keep training your current gym freely. Toggle it from the floating switch at the top of the gym page (beside the City and Tutorial buttons) or from the settings menu, with an optional sub-setting to exclude specialist gyms.
- **Diamond Days**: Added a brand new achievement tier for reaching 2000E+ training days, complete with new jewel visuals, weekly bar progress segments, hover animations for stickers and jewels, and enhanced bonuses for reaching this training goal. Diamond days award more points toward weekly progress, allowing standard and premium rewards to be achieved more easily. Points from all tiers contribute to an arbitrary leveling system in development to enhance long-term motivation and progression.
- **Achievements Page Overhaul** (in progress): Removed the expended energy category from the achievements page to include other more interesting statistics — including happy jump gains records and Diamond Day tracking, providing better insight into training patterns and special achievements.
- **Feature Guide** (in progress): Added a new feature guide offering transparency and support for the many features this script provides, making the extensive functionality more accessible to new and existing users.
- **Full Training Item Tracking**: Stat enhancers, energy items, and happy items used during training are now fully tracked in your logs, providing deeper insights and more training metrics.
- **Dynamic Item Counter**: View your daily, weekly, monthly, and yearly item usage in real-time with a context-aware counter that dynamically displays relevant items when appropriate.

### Bug Fixes:
- **Graph Tooltip Month Label (Monthly View)**: Fixed a bug where hovering over the monthly graph after navigating the calendar grid to a different month would display the grid's current month in the tooltip instead of the month the graph data belongs to. The tooltip now reads the month and year from values frozen at draw time (`selectedMonth`/`selectedYear` on the `dat` object returned by `_transformData`) rather than from the live `calendarState.month`/`calendarState.year`, which change whenever the user navigates the grid. This also covers the latent cross-year variant of the same bug.
- **Gold Week Sticker Award**: Fixed an issue where some gold weeks were not properly awarding 2 stickers instead of 1.
- **Sidebar Notification State**: Fixed an issue where the sidebar icon for the Gym Log would sometimes light up green when other pages received notifications, causing false positives.
- **TornPDA Update Flag**: Fixed an issue that prevented the update notification flag from appearing immediately after updating and refreshing on TornPDA.

### Improvements:
- **Performance Optimization**: The script has been comprehensively refactored to optimize performance across multiple open tabs and lower the resources consumed while the gym log panel isn't actively opened.
- **Dedicated Page Mode URL**: Page mode now navigates to a dedicated calendar URL (`calendar.php#gymlog`) rather than appending a hash to the current page. This prevents other scripts from throwing console errors when the DOM is manipulated, providing a cleaner and more stable environment.
- **Weekly Progress Bar Overhaul**: Completely overhauled the weekly progress bar system visually and functionally. The system now translates training into "points" (200 green / 300 gold / 500 diamond per day) for the future leveling system, making room for new diamond-tier bar segments. Gold and green fills are now anchored left and right for clearer visual hierarchy.
- **RPG Leveling Foundation & Balancing**: Reworked the weekly points system to establish the foundation for a training-integrated RPG leveling system. Points gained from happy jumping and daily goals have been re-evaluated and delicately balanced to foster a graceful progression scale that motivates and rewards consistent training efforts.
- **Achievements Copy/Paste Output**: Reworked the copy/paste output on the achievements page for both individual stats and full category copies. Category headers are now spaced and framed with em-dashes, multi-line stats are separated by blank lines while single-line stats stay grouped, and row labels are aligned with the on-screen UI for cleaner sharing on Discord and forums.
- **Expanded Date Tracking on Achievements**: Added date tracking to more achievements — the Best Training Streak, Best Green Streak, and Best Gold Streak rows now display their date ranges under the stat in expanded panel and page mode, matching the date styling used elsewhere on the achievements page.
- **Tap-and-Hold Parity with Desktop Hover**: Extended the mobile tap-and-hold interaction to fully imitate desktop "hover" behavior across the script, so tooltips, scrubbing, and other hover-driven affordances behave identically on touch devices — no functionality is lost between the two platforms.
- **Settings Configuration Reorganization**: Regrouped the settings menu into clearer, purpose-named sections — Big Black Features, Log Format, API Access, Data Management, and Information — for quicker navigation. Made the DEMO MODE button dynamic to function as an EXIT DEMO button while demo mode is active, enhancing navigation clarity and reducing user confusion.
- **Sidebar Branding Update**: Changed the SVG icon for the Gym Log from a flexing arm to a Crown, improving visual branding consistency across the application.
- **Happy Item Achievements & Insights**: Updated the achievements page to track happy item usage and added a summary of total happy gained from each item on the Happy Hopping page.
- **Expanded Copy-to-Clipboard**: Enhanced ledger copy functionality to allow copying a single stat by clicking or tapping its label. Expanded this feature to the Greatest Gains page, allowing individual stat columns to be copied to the clipboard for more dynamic stat sharing and recording.
- **General UI Refinements**: Various minor improvements across the interface for enhanced usability and visual polish.

--------------------------------------------------

Version 0.9.50 - 2026-05-13
### New Features:
- **Achievements Dashboard**: Introduced a dedicated achievements page that tracks and visualizes long-term training milestones and statistics for higher training satisfaction. It monitors record-breaking gains (per-click, daily, weekly, and monthly), total energy expenditure, and consistency streaks (active days, green goals, and gold goals). The dashboard also provides a summary of 'Rewards Reaped,' including total happy jumps and stickers unlocked, all displayed in a premium, tabular-aligned numeric interface.

### Bug Fixes:
- **Sidebar button and Notes/People/Settings panel interaction**: Repaired the sidebar Gym Log button and future-proofed both its injection and the panel/chat shove logic by anchoring everything to stable native identifiers (`#nav-gym`, `#chatRoot`, `#notes_panel_button`, `#people_panel_button`, `#notes_settings_button`, and the `channel_panel_button:CHANNEL-ID` pattern) instead of Torn's brittle, frequently rotating CSS class name hashes. The sidebar button now reads live container/row/link/icon classes from existing sibling nav elements at injection time, so styling stays in sync with whatever Torn currently uses. The shove math was reworked so the BBGL panel coexists cleanly with native Notes, People, and Settings panels: each chat panel is individually shifted via its `right` position (composes with Torn's own per-panel transform) while native panels stay in their Torn-managed slots, eliminating the overlap, off-slot stacking, and disappearance bugs that the previous parent-transform approach caused. Active-state class handling was also generalized so navigating away from the gym log correctly clears the active highlight on every nav item, not just the container.
- **Graph layout and scaling**: Fixed unstable graph rendering when opening the graph view—including the plot shifting right with a false left gutter, extreme text scaling, blank charts, and x-axis labels falling off-screen. Sizing now uses layout dimensions (`clientWidth` / `clientHeight`) so the SVG `viewBox` stays correct while the CRT-style open animation runs (bounding-rect reads were picking up transform scale and corrupting measurements). Y-axis margin is guarded against occasional inflated `getBBox()` results from font or layout timing by capping against a deterministic label-width estimate, so the inner plot stays aligned reliably across opens.

### Improvements:
- **Happy Jump Weekly Progress**: Users who train in short high-energy bursts ("happy jumpers") can now complete and gold their weekly bar without needing 5 standard training days. The happy jump detection window has been extended to 10 minutes (from 5). A happy jump day now fills 50% of the weekly bar instead of 20%—meaning 2 happy jumps in a week completes the bar and unlocks a sticker. If a happy jump day also reaches the 1500E gold threshold, its bar segment turns gold. A week with 3 or more happy jumps (green or gold) is automatically treated as a gold week, unlocking 2 stickers—while individual day cells remain their earned color. Detection is real-time and updates the bar live as training is logged.
- **Fluid responsive layout**: Reworked styling across the entire injected UI away from a hard split between "mobile" and "desktop" rules toward a fluid scaling system (container queries, shared custom properties, and clamp-based spacing and typography). Page mode, docked/expanded panel, ledger, graph, achievements, stickers, and shared chrome now scale on a continuum so layout stays consistent across viewport sizes and devices, with fewer brittle `@media` overrides and a leaner, easier-to-maintain stylesheet.
- **Early injection for navigation controls**: Changed how the Gym Log sidebar button and footer tab are mounted so they are inserted as soon as the userscript runs, before the host page finishes painting. That removes the visible "pop-in" where controls appeared a beat late and looked glitchy or unfinished.
- **Footer tab icon alignment**: Centered the Gym Log SVG in the notes footer tab by clearing the header logo’s right margin when that markup is reused in the tab (flex centering had been offset by the extra margin).
- **Overall script efficiency**: Reduced redundant work in hot paths (rendering, panel updates, and graph redraws) and tightened DOM/CSS patterns so the script's footprint on the page stays negligible in normal use—no perceptible slowdown while browsing Torn.
- **Developer Console Polish**: Added a branded `BBGL` console badge, a read-only `window.BBGL` debug surface (`BBGL.help()`, `BBGL.state()`, `BBGL.config()` with API-key redaction, etc.) for cleaner bug reports, User Timing performance marks visible in DevTools when dev mode is on.
- **Graph UI / y-axis range**: Further improved the graph UI by allowing tighter y-axis value ranges so the plotted lines use more vertical resolution and read as steeper slopes, addressing feedback from Wulfie.

--------------------------------------------------

Version 0.9.21 - 2026-05-05

### New Features:
- **Graph Projection Overhaul**: Re-engineered the graphing engine from a point-based coordinate model to a sectional, day-bucketed rendering system to more clearly display the start and end of buckets in each view. This includes a dynamic "All-Time" view that automatically cycles through tiers to show the entire history—from the log's origin to the current point—remaining visible and legible for up to 16 years of data.


### Bug Fixes:
- **High-Frequency Train Logging**: Fixed an error where clicking the train button multiple times within a single second caused only one entry to be recognized. The unique key now includes the "after" stat value to ensure rapid clicks are recorded distinctly (discovered by Svegarn).

### Improvements:
- **Prettier Session Sharing**: Redesigned the "Copy Session Data" output for a cleaner, vertical layout optimized for sharing on Discord and forums. Includes a new Crown header, categorized stat lines with emojis, and a dedicated status message for no-energy days.
- **Storage Optimization (Thin Cache)**: Redesigned the `sessionStorage` architecture to use a lightweight "thin cache" that strips out heavy `series` arrays. This significantly reduces the storage footprint and mitigates `QuotaExceededError` risks.
- **Redundant Data Removal**: Removed static `rate` fields from stored log entries in favor of dynamic on-the-fly calculations, further shrinking the overall database footprint.
- **Data Persistence Reliability**: Hardened the IndexedDB read guards to ensure log history is always reconciled against the database, preventing "blank calendar" rendering issues on page refresh.
- **Enhanced Rate Continuity**: Applied the mathematical bridging logic from the daily ledger to all period views (Week, Month, Year). Starting rates now anchor to the previous period's final value, ensuring seamless continuity across all time scales.
- **Optimized Log Export**: Refined the export log structure to significantly reduce file size while improving human legibility. Features include a semi-minified entry layout, day-bucketing, and clear timezone-aware sub-headers.
- **Toolbar Visibility Enhancement**: Improved view-toggle button visibility across all backgrounds, specifically addressing feedback from Svegarn regarding the bright Stickerbook page. Icons now use a persistent white color with smart opacity for inactive states and a dynamic scale-and-glow effect to clearly indicate the active view.


--------

Version 0.9.10 - 2026-04-25

### New Features:
- **Changelog System**: Implemented a one-time pop-up modal and sidebar notification system to alert users of script updates and new features.
- **Sponsorship Sticker Page**: Added a new "Sponsorship" page to the left of the existing sticker book. Accessible via a custom Gold and Shiny navigation arrow, the page features large jagged-edge slots for future faction sponsorships.

### Bug Fixes:
- **Ledger Rate Discrepancy**: Fixed issue where starting rates for a new day did not match the ending rates of the previous day. Switched to last-entry point rates for display to ensure mathematical continuity.
- **Image Asset Migration**: Hosted all image assets on a new platform to resolve rendering and access issues for users in specific regions (UK/International), including DonCorleone and ApexJP.
- **Expanded Panel UI**: Repaired the SVG for the expanded panel view toggle; the arrow inversion and compaction icons are now correctly visible and functional.
- **Critical Data Merge**: Fixed a critical bug that caused errors or data corruption when merging an old log export with a new one if a gap existed in the log history.

### Improvements:
- **UI/UX Layout Refinement**: Tweaked various UI elements to provide a cleaner, more professional layout across both mobile and desktop viewports.
- **Demo Mode Clarity**: Enhanced the "Exit Demo Mode" interface with clearer instructions and visual cues to ensure users can easily return to their real data (as per Svegarn's recommendation).
- **Page Mode Intelligence**: Added a context-aware tooltip to the log footer icon that indicates the panel version is disabled while viewing the log in full-page mode (as per Svegarn's recommendation).
- **Mobile Tooltip Scrubbing**: Re-engineered calendar tooltips for mobile devices. Users can now tap-and-hold a specific day to view battlestat snapshots and "scrub" (slide finger) to view other days. This mimics desktop hover functionality without obstructing the view (as per LatinoBull's recommendation).
- **Data Integrity Guardrails**: Hardened the import and export functions with robust IndexedDB connection handling. Added automatic database re-initialization, proactive integrity checks to prevent silent data loss, and clearer, actionable error messages to guide users through storage issues.
- **Enhanced Error Messaging**: Updated error codes to provide more clarity to users during malfunction. (Recognized the necessity due to Wulfie being a Boomer)
